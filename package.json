{
  "name": "github-mcp-vercel",
  "version": "1.0.0",
  "description": "GitHub MCP server deployable to Vercel — gives Cowork full GitHub access via URL",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.1",
    "@octokit/rest": "^20.1.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "typescript": "^5.4.5",
    "@vercel/node": "^3.0.0"
  },
  "engines": { "node": ">=18.0.0" }
}
